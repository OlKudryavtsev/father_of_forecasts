# v2.5 No prediction noise, rating points, current stage header

## Изменения

1. Отключены уведомления о том, что участник сделал/обновил прогноз на матч:
   - больше не отправляются администратору;
   - больше не отправляются в групповой чат.

2. Вкладка `Рейтинг`:
   - справа вверху карточки участника теперь сразу видно количество очков;
   - прогресс турнирного прогноза `4/4` перенесен под имя участника.

3. Верхний статус Mini App:
   - убран текст `До ЧМ чч:мм:сс`;
   - вместо него показывается текущий тур/стадия: `1 тур`, `1/8 финала`, `Финал` и т.д.

## SQL

Миграции не нужны.
