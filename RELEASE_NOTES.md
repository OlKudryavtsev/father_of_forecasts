## v3.9.0

- Добавлен турнир `ucl_2026_2027` — **ЛЧ 2026/27** — в селектор Mini App.
- Для ЛЧ добавлены долгосрочные прогнозы: победитель, финалист и лучший бомбардир; 3-е место отключено.
- Добавлена явная идемпотентная синхронизация матчей ЛЧ из API-Football через `python scripts/sync_ucl_2026_2027.py`.
- Выбор ЛЧ в Mini App больше не вызывает API-Football и не пишет матчи в БД.
- Для ЛЧ frontend скрывает 3-е место и не показывает WC-only кандидатов на бомбардира.
- Добавлена миграция `034_add_ucl_2026_2027_tournament.sql`.

## v3.8.0

- Добавлена таблица `tournaments` и регистрация текущего `wc2026` как отдельного турнира.
- Добавлен выбор турнира в Mini App и передача `tournament_code` в основные API.
- Добавлен read-only режим для завершённых и архивных турниров.
- Добавлен универсальный админский импорт архивного турнира через JSON.
- Кэш вероятностей победы разделён по `league_id + tournament_code`.
- Добавлена миграция `033_add_tournaments_and_archive_import.sql`.

## v3.7.11

- Исправлены финальные статусы и очки турнирных прогнозов: 3-е место Англии, красные неверные бомбардиры, фактические баллы вместо оставшегося потенциала.

## v3.7.10

- Исправлен нулевой показ турнирных очков после завершения турнира.
- Добавлен динамический пересчёт долгосрочных прогнозов от финального результата турнира.
- Исправлено сравнение русских названий команд с API-названиями и алиасов бомбардиров.
- Миграция БД не требуется.

## v3.7.9

- Финальная карточка победителя турнира прогнозов в «Матч-центре».
- В рейтинге убраны шансы/расклады после завершения турнира.
- Добавлен переключатель баллов: всего / матчевые / турнирные.
- Добавлена математика набора очков по каждому участнику.
- Отключены автоматические утренние турнирные рассылки новостей и итогов.


## v3.7.7

- Добавлена аналитика совпадений прогнозов по парам участников в разделе «Прогнозы».
- Доступны фильтры: учитывать/не учитывать проход и множественный выбор стадий.
- Учитываются только начавшиеся или завершённые матчи, где оба участника пары сделали прогноз.
- Миграция БД не требуется.

# v3.7.6 — Харри Кейн, статусы долгосрока и безопасный старт

- Исправлена нормализация русских имён игроков с буквой «й». Из-за неё «Харри Кейн» не сопоставлялся с `Harry Kane` в кэше бомбардиров.
- Для турнирных прогнозов и «Раскладов» национальная сборная из проверяемого справочника применяется даже при частичном ответе API. Кейн считается активным, пока Англия продолжает турнир.
- Долгосрочная ставка на бомбардира учитывает его текущие голы и остаётся живой, если его сборная ещё в игре.
- Версия модели кэша повышена: после деплоя расчёты «Раскладов» автоматически пересоберутся в фоне.
- Устранена гонка `Base.metadata.create_all()` при совместном запуске бота и Uvicorn: создание отсутствующих таблиц в PostgreSQL защищено transaction-scoped advisory lock.
- Заменён устаревший параметр FastAPI `regex` на `pattern`.

Миграция не нужна.

# v3.7.5 — Фоновый кэш вероятностей и PWA

- Рейтинг и «Расклады» получают готовые результаты симуляции из фонового PostgreSQL-кэша, а не запускают расчёт при открытии экрана.
- Бот пересчитывает данные при старте и затем раз в 3 минуты только при изменении входных данных.
- Исправлена постоянная плашка обновления PWA: версия JavaScript, Service Worker и серверной проверки синхронизируются из одного источника.
- Добавлена миграция `032_add_league_win_model_cache.sql`.

# v3.7.4 — Вероятности победы и вероятные пути к 1-му месту

## Рейтинг

- На карточке каждого участника добавлен показатель **«Шанс на 1-е»**.
- Это модельная вероятность именно единоличного первого места, рассчитанная одной общей симуляцией для всей активной лиги.
- Отец остаётся вне конкурса, поэтому показатель для его строки не выводится.

## Расклады

- Раздел по-прежнему свёрнут по умолчанию; выбор участника остаётся выпадающим списком.
- Вместо редких жёстко заданных комбинаций выводятся до 10 наиболее частых победных путей из симуляции.
- Пути отсортированы по убыванию их доли среди всех симуляций, в которых выбранный участник стал единоличным первым.
- Для каждого пути показаны: типичный диапазон итоговых и дополнительных очков, диапазоны точных счетов / исходов / проходов, часто срабатывающий долгосрок и типичные лимиты основных конкурентов.
- В карточке пути два разных показателя: **доля среди победных путей** и **общий шанс этого пути** среди всех симуляций. Это исключает ситуацию, когда главный видимый показатель почти везде был «<0,1%».

## Модель

- В одном прогоне одновременно моделируются все участники, поэтому для каждого прогона определяется только единоличный лидер; ничьи за первое место не засчитываются как победа.
- Используются текущие ИИ-прогнозы Отца, доступные футбольные данные, текущие прогнозы участников, проходы и только живые долгосрочные ставки.
- Для трофейных прогнозов внутри одного прогона используется общий результат категории, поэтому одинаковые ставки участников не получают несовместимые исходы.
- Модель включает всю оставшуюся сетку: 1/8, 1/4, 1/2, матч за 3-е место и финал.

Миграция БД не требуется.
