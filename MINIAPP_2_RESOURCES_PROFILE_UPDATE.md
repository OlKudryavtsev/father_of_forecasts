# Mini App resources/profile update

## Что изменено

1. Из профиля убран блок:

```text
Укажите, за какую сборную болеете
```

Он был неактивным и сейчас не используется.

2. Нижняя вкладка `Турнир` заменена на:

```text
Ресурсы
```

3. В раздел `Ресурсы` возвращены блоки из бывшего `Еще`:

- получение факта;
- карточка архива;
- полезные ссылки.

4. В этом же разделе оставлены кнопки:

- `Прогноз Отца прогнозов`;
- `Помощь по бомбардирам`.

## SQL

Миграции не нужны.
